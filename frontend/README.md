# Frontend

React + TypeScript + Vite 前端应用。

## 本地开发

```bash
cp .env.example .env.local
npm install
npm run dev
```

默认地址：`http://localhost:3000`

## 环境变量

- `VITE_API_URL`：后端 API 地址，默认建议 `http://localhost:3001/api/v1`

## 构建

```bash
npm run build
npm run preview
```
